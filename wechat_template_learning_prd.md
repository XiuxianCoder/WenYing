# 文映 WenYing：微信公众号文章模板学习与 Word 自动排版工具 PRD

## 0. 产品名称

中文名：文映

英文名：WenYing

名称含义：将用户的 Word 文稿映射为已发布公众号文章的排版风格，让内容保持原意，同时呈现出目标模板的视觉表达。

## 1. 产品背景

用户经常需要将已有 Word 文档发布为微信公众号文章。Word 文档中包含待发布的正文、标题、图片、表格、列表等真实内容，但微信公众号文章需要较强的视觉排版能力，例如标题样式、分割线、图片布局、引用框、重点卡片、结尾引导等。

现有痛点是：手工排版耗时，且很难复刻某些已发布公众号文章的成熟视觉风格。用户希望输入若干篇已发布公众号文章作为“参考模板”，系统自动学习其排版规律，再将自己的 Word 内容重新排成类似风格的公众号文章。

## 2. 产品目标

构建一个本地或 Web 工具，支持用户导入 Word 文档和参考公众号文章，自动生成适合微信公众号编辑器粘贴的排版内容。

核心目标：

- 解析 Word 文档中的真实内容和图片。
- 从已发布公众号文章中学习排版风格。
- 将 Word 内容自动重排为相似模板风格。
- 输出可预览、可微调、可复制到公众号后台的 HTML。
- 保留用户原文内容，不改变核心语义。

## 3. 核心用户场景

### 3.1 单篇模板学习

用户上传一个 Word 文档，并输入一篇已发布公众号文章 URL。系统分析模板文章的排版样式，然后将 Word 内容转换为类似风格的公众号排版。

### 3.2 多篇模板学习

用户输入多篇同一账号或同一风格的公众号文章。系统综合分析这些文章，提取稳定的排版规律，生成一个可复用模板。

### 3.3 模板库复用

用户将学习好的模板保存到模板库。后续只需上传新的 Word 文档，选择模板，即可快速生成排版文章。

### 3.4 人工微调

系统生成文章后，用户可以预览并调整标题、颜色、段落间距、图片位置、重点卡片、结尾引导等。

## 4. 功能范围

### 4.1 Word 内容解析

支持输入 `.docx` 文件，解析以下内容：

- 文档标题
- 一级标题、二级标题、三级标题
- 普通段落
- 加粗、斜体、下划线等基础富文本
- 图片及其原始顺序
- 图片附近说明文字
- 列表
- 表格
- 引用或重点段落

解析结果转换为内部结构化内容，例如：

```json
{
  "title": "文章标题",
  "sections": [
    {
      "heading": "小节标题",
      "paragraphs": ["段落1", "段落2"],
      "images": [{"id": "img_1", "caption": "图片说明"}]
    }
  ]
}
```

### 4.2 公众号模板文章采集

支持输入：

- 微信公众号文章 URL
- 用户上传的长截图
- 用户上传的多张页面截图

优先级：

1. 优先尝试解析公众号文章公开 HTML。
2. 如果 HTML 不完整或样式不可用，则使用截图进行视觉分析。
3. 最终生成统一的模板结构描述。

### 4.3 模板风格学习

系统需要从模板文章中提取：

- 文章整体风格：商务、科技、极简、教育、情感、活动、品牌宣传等。
- 主色、辅助色、背景色。
- 标题层级样式。
- 正文样式：字号、颜色、行高、段落间距、缩进。
- 图片布局：全宽图、圆角图、图注、左右图文、九宫格等。
- 特殊组件：引用框、重点提示框、编号块、分割线、按钮、结尾引导。
- 文章结构：开头导语、主体分节、案例卡片、总结、关注引导。

模板学习结果保存为模板 JSON：

```json
{
  "name": "蓝色商务科普风",
  "sourceUrls": ["https://mp.weixin.qq.com/..."],
  "style": {
    "primaryColor": "#2F6BFF",
    "textColor": "#333333",
    "fontSize": 16,
    "lineHeight": 1.9
  },
  "blocks": {
    "title": "大号加粗居中",
    "sectionHeading": "蓝色左竖线标题",
    "paragraph": "常规正文段落",
    "image": "圆角大图，居中展示",
    "quote": "浅蓝背景重点提示框"
  },
  "layoutRules": [
    "每个小节标题前加入装饰分割",
    "图片跟随最近的小节正文",
    "重点句可转换为提示框"
  ]
}
```

### 4.4 Word 内容自动重排

系统根据模板规则，将 Word 内容映射到公众号排版块：

- Word 主标题 -> 公众号文章标题区域
- Word 小标题 -> 模板小节标题
- 普通段落 -> 模板正文段落
- Word 图片 -> 模板图片块
- 图片说明 -> 图注
- 重点句 -> 重点卡片或引用框
- 表格 -> 微信兼容表格或图片化表格

内容原则：

- 不擅自改写用户正文。
- 可轻微调整段落拆分以适配阅读节奏。
- 图片顺序默认尊重 Word 原始顺序。
- 如果模板需要更多装饰图，默认不生成无关图片，只生成样式组件。

### 4.5 预览与编辑

生成后提供预览页面，支持：

- PC 预览
- 手机宽度预览
- 标题修改
- 颜色微调
- 字号、行高、边距调整
- 图片替换和顺序调整
- 重点卡片开关
- 重新生成布局

### 4.6 输出

MVP 输出形式：

- 可复制到微信公众号编辑器的 HTML。
- 本地预览 HTML 文件。
- 图片资源目录。

后续可扩展：

- 一键复制富文本到剪贴板。
- 自动打开微信公众号后台并粘贴草稿。
- 导出为 Markdown、PDF、长图。

## 5. 非功能需求

### 5.1 稳定性

- 模板分析失败时，应给出可读错误和人工补救入口。
- URL 抓取失败时，应允许用户上传截图继续。
- Word 图片提取失败时，应标记缺失位置。

### 5.2 安全与合规

- 不绕过微信登录、验证码、访问限制。
- 不自动发布文章，发布前必须由用户手动确认。
- 不保存用户账号密码。
- 模板学习应学习排版规律，避免直接复制第三方文章内容。

### 5.3 可维护性

- 模板以 JSON 存储。
- 排版组件模块化。
- Word 解析、模板分析、内容映射、HTML 渲染分层实现。

## 6. MVP 版本范围

### 6.1 MVP 必做

- 上传 `.docx` 并解析正文和图片。
- 输入 1 篇公众号文章 URL 或上传截图。
- 生成模板结构 JSON。
- 将 Word 内容套入模板。
- 生成可预览 HTML。
- 支持复制 HTML 到公众号编辑器。
- 支持保存模板。

### 6.2 MVP 暂不做

- 自动登录微信公众号后台。
- 自动发布文章。
- 完美复刻任意公众号文章。
- 复杂动效、视频、小程序卡片。
- 多人协作。
- 云端素材库。

## 7. 技术方案

### 7.1 推荐架构

前端：

- React / Vue / Next.js
- 预览编辑器
- 模板选择和参数调整面板

后端：

- Node.js 或 Python
- Word 解析服务
- 模板分析服务
- HTML 渲染服务

AI 能力：

- 视觉模型分析截图
- 文本模型总结模板规则
- 内容结构识别和映射

### 7.2 Word 解析

可选方案：

- Node.js：`mammoth.js`
- Python：`python-docx`
- 图片提取：读取 docx zip 内部 `word/media`

建议：

- MVP 使用 `mammoth.js` 快速提取 HTML。
- 对图片做资源落盘并建立映射。

### 7.3 模板采集

URL 方式：

- 使用 Playwright 打开公众号文章。
- 获取页面 HTML、截图、图片尺寸。
- 提取文章主体区域。

截图方式：

- 用户上传长截图。
- 视觉模型识别布局块。
- 生成模板描述。

建议：

- URL 和截图同时支持。
- URL 成功时使用 HTML + 截图双重分析。
- URL 失败时退回纯截图分析。

### 7.4 模板学习

模板学习流程：

1. 识别文章结构块。
2. 提取样式特征。
3. 归纳可复用排版规则。
4. 生成模板 JSON。
5. 渲染一篇样例预览供用户确认。

### 7.5 HTML 渲染

公众号编辑器兼容性要求：

- 使用内联 CSS。
- 避免复杂外链样式。
- 尽量使用 `section`、`p`、`span`、`img`。
- 控制宽度、边距、行高。
- 图片使用可访问 URL 或本地上传后的 URL。

## 8. 数据结构设计

### 8.1 DocumentContent

```ts
type DocumentContent = {
  title: string;
  blocks: ContentBlock[];
  images: ImageAsset[];
};
```

### 8.2 ContentBlock

```ts
type ContentBlock =
  | { type: "heading"; level: number; text: string }
  | { type: "paragraph"; text: string; marks?: TextMark[] }
  | { type: "image"; imageId: string; caption?: string }
  | { type: "quote"; text: string }
  | { type: "list"; items: string[] }
  | { type: "table"; rows: string[][] };
```

### 8.3 LearnedTemplate

```ts
type LearnedTemplate = {
  id: string;
  name: string;
  sourceType: "url" | "screenshot" | "mixed";
  sourceRefs: string[];
  styleTokens: StyleTokens;
  blockStyles: Record<string, BlockStyle>;
  layoutRules: string[];
};
```

## 9. 用户操作流程

1. 用户上传 Word 文档。
2. 用户输入公众号模板文章 URL 或上传截图。
3. 系统解析 Word 内容。
4. 系统分析模板文章风格。
5. 系统生成模板 JSON。
6. 系统将 Word 内容映射到模板。
7. 用户预览生成结果。
8. 用户微调。
9. 用户复制 HTML 到公众号后台。
10. 用户手动检查并发布。

## 10. 风险与解决方案

### 10.1 公众号 URL 抓取不稳定

解决：

- 支持截图分析。
- 支持用户手动上传 HTML。
- 支持人工模板微调。

### 10.2 视觉分析无法精确还原 CSS

解决：

- 不追求像素级复刻。
- 输出“相似风格”的可编辑模板。
- 用预览和人工微调闭环。

### 10.3 微信编辑器兼容性问题

解决：

- 使用内联 CSS。
- 建立微信兼容组件库。
- 每个模板保存前做粘贴测试。

### 10.4 图片处理复杂

解决：

- MVP 保持 Word 图片原顺序。
- 后续增加智能图片匹配。
- 支持用户手动拖拽调整。

## 11. 版本规划

### V0.1 原型

- Word 解析
- 单模板截图分析
- 生成预览 HTML

### V0.2 MVP

- URL + 截图混合分析
- 模板 JSON 保存
- Word 自动套模板
- HTML 复制

### V0.3 增强版

- 多模板学习
- 预览编辑器
- 图片位置智能调整
- 重点句自动卡片化

### V1.0 正式版

- 模板库
- 批量生成
- 微信编辑器粘贴自动化
- 团队共享模板

## 12. 验收标准

MVP 验收：

- 能成功解析包含文字和图片的 Word 文档。
- 能从至少一种公众号模板输入方式生成模板 JSON。
- 能生成一篇结构完整、视觉风格接近模板的公众号文章。
- 生成 HTML 能在微信公众号编辑器中基本保持样式。
- 用户可以在 5 分钟内完成一次 Word 到公众号排版转换。

## 13. 开放问题

- 模板文章是否主要来自固定几个公众号，还是任意公众号？
- Word 图片是否需要自动上传到微信素材库？
- 是否需要保留 Word 中的表格原样？
- 生成结果是只要复制 HTML，还是必须自动创建公众号草稿？
- 模板学习更看重整体风格，还是要求尽量接近原模板每个模块？

